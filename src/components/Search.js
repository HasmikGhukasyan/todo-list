import Button from "./Button";

const Search = () => {
    return (
    <div>
      <input type = "text" placeholder="Type text for"/>
      <Button text="Search" color="Blue"/>
      </div>
  )}

  export default Search;